name: Keep Render Awake
on:
  schedule:
    - cron: '*/12 * * * *'
  workflow_dispatch: {}
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sf --max-time 90 --retry 3 --retry-delay 20 https://budgetflow-wpdg.onrender.com/health
